/**
 * tree_build_by_layer
 * 
 * @export
 * @param {any} tree 
 */
export function tree_build_by_layer(tree) {
    if (typeof tree !== 'array') throw Error('tree data mast be a array');
    tree.map((t, i) => {

    })
}